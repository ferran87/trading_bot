# Pins chosen to have pre-built Python 3.14 wheels on Windows as of Apr 2026.
# Loose upper bounds so pip can pick whatever's current.

# Broker / market data
yfinance>=0.2.54
requests>=2.32.0           # Trading 212 REST broker (also transitive dep of yfinance)

# Core
pandas>=2.3.0
numpy>=2.2.0
SQLAlchemy>=2.0.36
psycopg2-binary>=2.9.9          # PostgreSQL driver (Supabase / Streamlit Cloud)
pydantic>=2.10.0
python-dotenv>=1.0.1
PyYAML>=6.0.2

# Phase 3 (sentiment) — installed now, used later
anthropic>=0.42.0
feedparser>=6.0.11

# Phase 2 — AI Thesis Bot earnings tools
lxml>=5.3.0                # required by yfinance Ticker.earnings_dates DataFrame
edgartools>=5.30.0         # SEC EDGAR 8-K filings (management guidance)

# Dashboard
streamlit>=1.41.0
plotly>=5.24.1

# Dev / test
pytest>=8.3.4
pytest-cov>=6.0.0
freezegun>=1.5.1
